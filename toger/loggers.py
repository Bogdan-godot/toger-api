import logging

bot = logging.getLogger("toger.bot")
types = logging.getLogger("toger.types")
event = logging.getLogger("toger.event")
middlewares = logging.getLogger("toger.middlewares")
webhook = logging.getLogger("toger.webhook")
scene = logging.getLogger("toger.scene")